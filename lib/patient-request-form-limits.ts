/** Aligné sur la BDD (CHECK `client_comment` ≤ 500). */
export const PATIENT_PRODUCT_LINE_COMMENT_MAX = 500;

/** Note générale patient : borne UI raisonnable (colonne `text` côté serveur). */
export const PATIENT_GENERAL_NOTE_MAX = 1000;
