/** Carte ligne produit dans un groupe bucket (vues répondue et validée). */
export const patientBucketProductRowClass =
  "w-full min-w-0 rounded-lg border border-border/75 bg-card px-2 py-2.5 shadow-sm";

/** Liste de lignes dans un bucket — espacement entre produits. */
export const patientBucketProductListClass = "flex w-full min-w-0 flex-col gap-2 p-2";
