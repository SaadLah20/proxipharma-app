<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

Demande « produits » (MVP), ordre d’affinage UI par statut et règles dossiers figés : **`CAHIER_DES_CHARGES.md` §4.6**.

Après validation patient : le dossier reste **`confirmed`** pendant la saisie réservé/commandé, puis **`treated`** dès que la pharmacie valide la préparation pour le comptoir (RPC **`pharmacist_mark_request_treated`**), colonne **`request_items.withdrawn_after_confirm`**, table **`request_supply_amendments`** et RPC associées — voir **`CAHIER_DES_CHARGES.md` §4.4** et migrations **`20260508_002_processing_treated_supply_workflow.sql`**, **`20260512_002_remove_request_processing_status.sql`**. Le hub affiche **`in_progress_virtual`** quand le dossier est **`confirmed`** avec **`post_confirm_fulfillment`** avancé sur au moins une ligne retenue.

**UI ordonnances (juin 2026, branche `fix/validated-supply-ecart-ui-modal`)** — même parcours que demandes produits après réponse pharma ; thème **ambre** (`lib/request-kind-ui-theme.ts`, `lib/prescription-ui-copy.ts`) ; libellés **Ordonnance** / **Qté prescrite** ; historique saisie officine ; hubs **`RequestKindHubDashboard`** + cartes produits ; scan **`PrescriptionScanCollapsible`** (patient replié ; pharmacien **au-dessus** des lignes en saisie + **Voir l'ordonnance** / **Ajouter**). **Archive clôturée patient** : pas de récap **`RequestKindHeader`** au-dessus du bandeau dossier. **Validée supply** : dispo **À commander** → champ **Réception prévue** obligatoire en édition ligne (`pharmacistSupplyDraftNeedsReceptionDate`). Migrations **`20260703_001`**, **`20260703_002`**, **`20260707_002`**, **`20260708_001`**. Phrase reprise **§13.43** · commits **`721c991`**, **`112b679`**, **`6cb3160`**.

**UI patient demande produits (juin 2026, même branche)** — roadmap §4.6 : **envoyée**, **répondue**, **validée**, **traitée**, **archives** (sessions **2026-06-02** + **2026-06-03 suite 3**, phrase **§13.36**). **Saisie publique** : `patient-demande-produits-ui.tsx` — message vocal optionnel **30 s** via **`ConversationMessageDraftField`** (commit **`cb90da3`**). **Bandeau officine** : **`PatientPharmacyDossierBand`** (icône **Store**, **Contacter**, **Itinéraire**, **Voir la fiche** primary) — header dossier + récap envoyée. **Envoyée** : **`PatientProductRequestDossierHeader`** + **`PatientProductRequestCompactLine`**. **Répondue** : **`RespondedPatientLineChooser`** + **`lib/patient-responded-line-buckets.ts`** ; alternatives **sans présélection** ; barre **Retenir cette alternative** (onglets = cases indicatives). **Validée** (`confirmed`) : **`PatientValidatedCompactLineCard`** ; blocs **À réserver** / **À commander** ; **`PatientPharmaUpdateBanner`** ; **Modifier ma validation** (RPC **`patient_update_confirmation`**, migration **`20260625_001`**). **Traitée** (`treated`) : blocs réservés/commandés ; pastilles suivi sans « réservé/commandé » sur carte. **Archives** : **`PatientClosedArchiveBucketSection`** ; notes via **`PatientLineNotesIconButton`** + **`AppModalOverlay`**. **Hubs** : **`RequestKindHubDashboard`**. Détail historique session **2026-05-25** : **§13.33**.

**UI pharmacien demande produits (juin 2026, même branche)** — **`PharmacistProductRequestDossierHeader`** ; détail **`app/dashboard/pharmacien/demandes/[id]/page.tsx`** ; **validée/traitée** : **`PharmacistValidatedBucketSection`** (blocs arrondis unifiés) + **`PharmacistSupplyCompactLine`** (aligné patient, libellés bas, CTA traitée en ligne) ; **traitée** : titres **pour le patient**, pas de **`RequestLineSuiviStrip`** ; footer **Déclarer traitée** compact + **`InfoHint`** portail ; **archives** : **`PharmacistClosedProductBucketsView`** + **`pharmacist-closed-archive-line.tsx`** ; **saisie/répondue** : cartes compactes, onglet **Proposé**, **`lib/ui-density.ts`** ; footers sticky mobile (`platform-sticky-footer`) ; modales **`AppModalOverlay`**. Hub : **`PharmacistProductDemandeHubCard`**.

**Mes pharmacies patient (mai 2026)** — migration **`20260626_001`** ; **`/dashboard/patient/pharmacies`**, **`patient-pharmacies-directory.tsx`**, **`patient-pharmacy-detail.tsx`** ; RPC **`patient_pharmacy_directory_enriched`** / **`patient_pharmacy_detail`** ; **`PatientAccountPageHeader`** + **`lib/platform-dashboard-chrome.ts`** (charte compte, pas sky dossiers).

**Mes paramètres patient (mai 2026)** — **`/dashboard/patient/parametres`** · **`patient-settings-page.tsx`** ; menu **Mes paramètres** ; suppression compte **`POST /api/patient/delete-account`** (bloquée si demandes/promos actives).

**Mes paramètres pharmacien (mai 2026)** — **`/dashboard/pharmacien/parametres`** · **`pharmacist-settings-page.tsx`** + **`PharmacistSettingsSection`** ; menu **Mes paramètres** ; sections profil, officine (raccourcis fiche/horaires/pricing/promos), connexion/MDP/e-mail, notifications (**`ExternalNotificationPrefs`** `variant="pharmacien"` `appearance="settings"`), déconnexion (suppression compte = admin pilote). **Charte compte/officine** : **`lib/platform-dashboard-chrome.ts`** + **`PharmacistAccountPageHeader`** / **`PatientAccountPageHeader`** (tableau de bord, hubs liste, clients, pharmacies, paramètres, packs promo, notifications, supply, ma-fiche, promos, pricing, horaires) — **pas** le bleu sky des **dossiers** produits (détail dossier = sky/amber/violet selon type). Menu header : **Notifications** + **Annuaire** (patient) — **`platform-header.tsx`** (`patientNavMenu`, `pharmacienNavMenu`).

**Refonte UX Glovo-like (mai 2026) — ABANDONNÉE (2026-06-01)** — expérience « big bang » sur branche **`design/ux-refonte-2026`** (commits historiques **`3b189a7`**, **`89b35d1`**, jamais mergés) ; branche distante **supprimée**. Ne pas recréer ni merger ce lot. **UI/UX** : affinages **incrémentaux** sur **`fix/validated-supply-ecart-ui-modal`** (écran par écran, preview PR). Travail courant / prod = cette branche. Phrase contexte seul : **`CAHIER_DES_CHARGES.md` §13.35**.

**Sauvegarde pharmacien post-validé** — **`post_confirm_fulfillment`** : envoyer l’enum **`unset`**, jamais **`null`** (colonne NOT NULL) ; le clamp à l’enregistrement suit la dispo **déduite du brouillon** (même règle que **`buildItemUpdatePayload`** / **`effectiveAvailSupplyDraft`**, y compris avec **alternative choisie** — **`inferredAvailabilityForPostConfirmClamp`** dans **`app/dashboard/pharmacien/demandes/[id]/page.tsx`**).

**UI pharmacien post-validé** — brouillon (réservé/commandé, écart, comptoir **en attente / récupéré**) validé par **Enregistrer les modifications** via **`PharmacistSupplyCompactLine`** (voir aussi paragraphe UI pharmacien demande produits ci-dessus) ; pas d’auto-save comptoir ; migrations **`20260509_001`** / **`20260509_002`** si infra à jour.

**UI pharmacien — note / échange par ligne** — **`components/pharmacist/pharmacist-line-conversation-chip.tsx`** (`PharmacistLineConversationModal` sur **`app/dashboard/pharmacien/demandes/[id]/page.tsx`**) : **Confirmer la note** valide le texte saisi dans **`pharmacist_comment`** (pas de raccourci qui insère `"OK"`). Envoi patient = **publier la réponse** dossier.

**Fiche digitale pharmacie (mai 2026)** — **`CAHIER_DES_CHARGES.md` §10 (session **2026-05-24** + 2026-05-22 promo)** · phrase reprise **§13.36** (annuaire/fiche **§13.31**). Migrations **`20260606_001`** … **`20260610_001`** + **`20260622_001`** (`titular_public`). Publique **`/pharmacie/[id]`** : onglets grille 4 col., Infos, **Offres**, Horaires, **`PharmacyRatingForm`** ; titulaire si **`titular_public`**. Pharmacien **`ma-fiche`** : onglets **Coordonnées** (nom, adresse, tél., WhatsApp), **Accueil** (message + titulaire + Visible/Masqué), Photos, Liens, Services · **`horaires-garde`**. Onboarding admin : **`POST /api/admin/onboard-pharmacy`** — titulaire prérempli à la création. Storage : **`20260524_001`** + **`20260528_001`** + **`20260609_001`**.

**Reset pilote** — `supabase/scripts/reset-pilot-keep-products-single-admin.sql` (tout le fichier, modale « Run without RLS ») ; storage catalogue+officines : `node --use-system-ca scripts/reset-storage-keep-catalog-and-pharmacy-photos.mjs --confirm`.

**Vidage demandes (tests)** — SQL `supabase/scripts/clear-all-requests.sql` puis `node --use-system-ca scripts/clear-request-private-media.mjs --confirm` (garde `public-assets/products/` + `pharmacies/`). Détail **`CAHIER_DES_CHARGES.md` §0.1**.

**Version stable de référence (mai 2026)** — commit **`0c4f0e7`**, tag Git **`pilote-stable-2026-05-24`**, détail **`CAHIER_DES_CHARGES.md` §10.1**.

**ESLint / React** — règle **`react-hooks/set-state-in-effect`** (CI en erreur) : ne pas appeler **`setState` dans un `useEffect`** pour resynchroniser un formulaire (modale, passage patient, etc.). Préférer **remonter l’état dans un sous-composant** monté avec **`key={…}`** quand la modale s’ouvre (ex. **`PromoReserveForm`** dans **`promo-reserve-modal.tsx`**) ou réinitialisation en rendu si les props changent (voir journal §10 session patient **2026-05-14**).

**Packs promo (mai 2026, hub juin 2026)** — migration **`20260610_001`** : offres **`pharmacy_promo_offers`** (max 5 produits + 5 cadeaux, remise %), réservations **`pharmacy_promo_reservations`** (statuts `submitted` → `confirmed` / `unavailable` / `collected` / `cancelled`), refs **`P042/26`**, notifs **`promo_in_app_notifications`** (cloche header fusionnée avec **`app_notifications`** dans **`platform-header.tsx`**). Routes : pharmacien **`offres-promos`**, **`reservations-packs`** ; patient **`packs-promo`** ; libs **`lib/promo/`** (dont **`reservation-hub-buckets.ts`**, **`reservation-hub-sections.ts`**), UI **`components/promo/`** (**`PromoReservationsHubDashboard`**, **`promo-reservation-hub-card.tsx`**, **`PromoStatDashboard`**) — hubs **tableau de bord + liste filtrable** (5 tuiles statut, cartes hub, filtres) alignés demandes produits ; i18n patient **`promo.dashboard.*`**. RPC : `patient_submit_promo_reservation`, `pharmacist_confirm_promo_reservation`, `pharmacist_decline_promo_reservation`, `pharmacist_mark_promo_reservation_collected`, `cancel_promo_reservation`. Pas de SMS externe promo au pilote (in-app seulement). **Préfixe « Pharmacie »** patient : **`pharmacyPublicLabel`**, **`formatPatientNotificationPharmacyText`** (`pick-notification-text.ts`). Journal **`CAHIER_DES_CHARGES.md` §10 session 2026-06-06 (suite 9)** · phrase **§13.50**.

**Supply post-validé (mai 2026)** — **`app/dashboard/pharmacien/demandes/[id]/page.tsx`** (commit **`0094611`**) : qté validée + brouillon au reload ; ajouts officine **`confirmed`/`treated`** en **`pendingProposalRows`** jusqu’à Enregistrer.

**Moteur de pricing officine (mai–juin 2026)** — migrations **`20260619_001`** + **`20260713_001`** (règles **marque**, remplace laboratoire). Tables **`pharmacy_pricing_*`** + **`pharmacy_pricing_brand_rules`** ; RPC **`resolve_pharmacy_product_unit_price`**, **`pharmacist_pricing_distinct_brands`**, config get/save ; **`lib/pharmacy-pricing/`** ; UI **`/dashboard/pharmacien/pricing`** — onglet **Marques**. **Médicaments** = **PPV** fixe ; **parapharmacie** = PPH + marge **−10 % à +40 %** (global / **marque** / produit). Priorité : produit > marque > global. Affichage patient = **Prix / PU** uniquement. Journal **`CAHIER_DES_CHARGES.md` §10 session 2026-06-06 (suite 2)**.

**Ne pas réécrire tout le détail produit** sans relire **`CONTEXTE.md` §6** et **`CAHIER_DES_CHARGES.md` §4.4–§4.5 + dernier §10 Journal + §13.49** (phrase de reprise ; §13.12 / **`20260509_*`** pour supply post-validé). Pour une **ouverture sans tâche** : **`CAHIER_DES_CHARGES.md` §13.35**.

**Catalogue & photos (mai 2026)** — **`lib/storage-media.ts`**, **`lib/patient-demande-produits-draft.ts`**, **`/pharmacie/[id]/demande-produits/catalogue`** — explorateur patient : **`ProductCatalogExplorerToolbar`** (recherche + type para/médicament + **marque** en modale) ; liste plein écran scrollable ; scroll infini **`useProductCatalogExplorer`** (60/lot, **`fetchInFlightRef`**) ; RPC **`pharmacist_pricing_distinct_brands`** ; cartes **`ProductCatalogExplorerListRow`** ; i18n **`demandePublic`**. Vignettes = Storage **ou** URL externes (**`resolvePublicMediaUrl`**). Pilote juin 2026 : BeautyMall **`photo_url`**. Journal **§10 session 2026-06-06 (suite 8)** · phrase **§13.49**.

**Catalogue BeautyMall (juin 2026)** — chaîne **`scripts/README-beautymall-catalog.md`** : sitemap → fusion CSV → wipe SQL → **`import-beautymall-catalog.mjs`**. Pilote : **13 651** parapharmacie, **`full_description`** HTML, **12 171** photos URL. Détail **`CAHIER_DES_CHARGES.md` §10 session 2026-06-04 (suite 2)** · phrase **§13.39**.

**Médicaments officine (juin 2026)** — Excel TVA=0 · **`scripts/README-medicaments-officine.md`** · **`convert-medicaments-xlsx.py`** + **`import-medicaments-officine.mjs`** (additif). Pilote : **6 026** médicaments (`product_type = medicament`, PPH/PPV, sans photo). **Total catalogue** : **~19 677**. Journal **§10 session 2026-06-06 (suite 4)** · phrase **§13.45**.

**Marques catalogue (juin 2026)** — migration **`20260710_001`** · extraction **`scripts/extract-product-brands.py`** + **`scripts/README-product-brands.md`** (**v2.1 ~93,65 %** sur parapharmacie, appliquée Supabase). **`ProductBrandLabel`** catalogue + modale photo ; pricing par **marque** (**`20260713_001`**).

**Aperçu photo produit + description (juin 2026, commits `736100f`, `aea9fd4`)** — vignette cliquable **patient et pharmacien** → **`PatientProductPhotoPreviewModal`** (photo réelle si URL + panneau description scrollable ; image cadrée **`object-contain`** sans débordement). Placeholder « disponible prochainement » **sans photo** uniquement. **`ProductRequestLinePanel`** : lignes produit **flex RTL** (**`4989304`**). **`PharmacistProductPhotoThumb`** · **`lib/product-description-html.ts`** · **`lib/fetch-product-descriptions-map.ts`**. Journal **§10 session 2026-06-06 (suite 7–8)** · phrase **§13.49**.

**Types de demande (registre)** — **`lib/request-kinds/`** : config par type (`product_request`, `prescription`, `free_consultation`) — thème header, routes hubs, capacités, refs **D/O/C**. Workflow commun → **`lib/request-kinds/shared-capabilities.ts`** + **`lib/demandes-hub-buckets.ts`**. **Ordonnances** : après réponse = parcours demande produits ; saisie scan = **`PHARMACIST_AVAILABILITY_OPTIONS`** (indispo / rupture OK) via **`pharmacistAvailabilityOptionsForLine`** + modal ordonnance ; badges **Ordonnance** / **Proposé** ; ajout post-validé → **`request_supply_amendments`** + badge **« Ajouté après validation »** (`lib/supply-line-post-confirm.ts`). **Consultation libre** : détail **scroll unique** (brief + **`RequestConversationInline`** + lignes) ; recherche catalogue pharma : **`propCatalogSearchActive`** inclut **`free_consultation`** (formulaire sans accordéon). **Patient `responded`** : qté **Proposé** = `available_qty` (y compris alternatives — **`maxQtyAlt`**) ; peut diminuer. **Post-validé** : cartes = **`selected_qty`** ; amendement qté officine → sync **`selected_qty`** à l’enregistrement pharmacien. Anti-doublon catalogue : **`lib/pharmacist-request-catalog-product-block.ts`**. Migrations : **`20260603_001`**, **`20260604_001`** (notif patient mise à jour validée). Phrase de reprise : **`CAHIER_DES_CHARGES.md` §13.26**.

**Livraison (utilisateur non dev)** — `.cursor/rules/delivery-workflow-user.mdc` : l’agent fait branche + **PR vers `main`** + commit/push ; l’utilisateur attend la **preview Vercel**, teste, puis **Merge** sur GitHub. Après merge d’une PR, rouvrir une PR pour la suite sur la branche (sinon pas de preview auto).

**Développement** : le pilote est **toujours en dev** — si une correction est nettement plus simple en **réinitialisant les données** (script reset demandes / base de test), **demander explicitement** une vidage plutôt que d’empiler migrations ou branches SQL uniquement pour préserver des jeux obsolètes.

**Auth locale (SMS / Supabase)** : tester **`/auth`** dans **Chrome ou Edge** (fenêtre système), **pas** le navigateur intégré / Simple Browser de l’IDE — stockage, cookies et requêtes vers **`*.supabase.co`** y sont souvent incomplets ; si ça marche dans Chrome, le flux est en général correct.

**Fiche publique & annuaire (mai 2026)** — chrome partagé **`components/pharmacy/pharmacy-public-chrome.tsx`** ; annuaire **`components/annuaire/`** (avis sur carte, menu rayon portail) ; fiche **`pharmacy-public-profile.tsx`** + demande produits **`app/pharmacie/[id]/demande-produits/`** ; badge **Ouverte** (vert) / **Fermée** et libellés « fermé » horaires = rouge uniforme **`lib/pharmacy-open-status-ui.ts`** ; reprise **§13.31** / journal §10 session **2026-05-19 annuaire + fiche**.

**Auth patient (mai 2026)** — **`app/auth/page.tsx`** :
- **Connexion** (`/auth`) : un champ **téléphone ou e-mail** + mot de passe (`lib/auth-login-identifier.ts`, `signInWithPassword`).
- **Inscription** (`/auth?mode=signup`) : nom + téléphone + e-mail facultatif → **OTP** (souvent **SMS** ; au pilote peut arriver via **WhatsApp** selon config Supabase/Twilio Verify) → mot de passe → session (`lib/ensure-patient-profile.ts`, `lib/phone-e164.ts`). Numéros **pro** : SMS Verify souvent **Failed** — tester en **perso** (`+2126…`).
- E-mail récupération : **`/auth/update-password`** ; **mot de passe oublié** connexion : **SMS** si identifiant = téléphone (`lib/auth-phone-password-reset.ts`), **lien e-mail** si identifiant = e-mail ; opt-in e-mail aussi dans **`app/dashboard/patient/parametres/page.tsx`**.
- **Pharmacien provisionné** (admin) : première connexion → choix MDP — **`lib/provisioned-pharmacist-auth.ts`**, `?provisioned=1` sur **`/auth`**.
- Migration **`20260521_001_profiles_email_nullable.sql`** : `profiles.email` nullable.
- Unicité : **téléphone / e-mail** uniques côté **`auth.users`** (Supabase) ; doublons possibles dans **`profiles.whatsapp`** sur comptes **legacy** (e-mail avant SMS).
- **Inscription** : avant envoi OTP, **`POST /api/auth/signup-phone-check`** + **`auth_phone_user_exists`** (**`20260522_003`**) — pas de SMS si le numéro est déjà dans **`auth.users`** ; message « utilisez Connexion ». **Renvoi OTP** : `shouldCreateUser: false`. Doublons historiques même téléphone en pilote : reset demandes + suppression comptes Auth.

**Notifications hors-app (e-mail + SMS)** :
- File SQL : **`notification_external_queue`**, prefs **`notification_external_prefs`**, trigger sur **`app_notifications`** (`20260505_001`). SMS/WhatsApp : destination = **`profiles.whatsapp`** (E.164).
- Workers : **`send-external-emails`**, **`send-external-sms`**, **`/api/webhooks/dispatch-external-sms`** (principal, ~secondes) ; **`lib/external-notification-queue-worker.ts`** (SMS 1 tentative, texte court anti **Twilio 30007**, `SMS_BLOCKED_DESTINATIONS`).
- **SMS pilote (patient uniquement)** : enqueue **`request_status:responded`** et **`request_status:treated`** seulement (**`20260522_001`**, **`20260522_002`**). Pas de SMS pharmacien (UI prefs **`variant="pharmacien"`**). Texte : `ProxiPharma: {officine} a repondu. Dossier {request_public_ref}.` / `… a traite le dossier {ref}.` — ASCII, 1 segment, ref **`D042/26`** (pas le libellé type « Demande de produits »).
- **Destination notif SMS** : **`profiles.whatsapp`** (E.164) — pas `auth.users.phone` (legacy e-mail OK sans téléphone Auth).
- **Vercel** : `TWILIO_*` + `TWILIO_SMS_FROM` (ex. `+19789813065`, API Messages) ; `SMS_BLOCKED_DESTINATIONS` pour numéros test invalides.
- **Webhook Supabase** (INSERT file, même URL) = **e-mail + SMS rapides** (canal de la ligne insérée). Cron GitHub = filet (~5 min). E-mail = lien complet ; **SMS = sans URL** (30007 si lien long).
- Détail / reprise : **`RUNBOOK.md` §9**, **`CAHIER_DES_CHARGES.md` §10 (2026-05-16 + 2026-05-22)**, phrase **§13.16**.

**Notifications WhatsApp (en cours, pas encore de worker)** :
- **Étape 1 (infra, avant code)** : Meta Business + expéditeur WhatsApp via Twilio ; test manuel **template** depuis la console ; **ne pas** utiliser **MM Lite** pour messages utilitaires (erreur Twilio **63055** → envoyer via **Cloud API**, modèles catégorie **Utility** pour statuts demande).
- **Numéro SMS USA Twilio ≠ expéditeur WhatsApp** ; **pas** d’envoi API depuis le WhatsApp perso du pharmacien — liens **`wa.me`** OK sans Meta Business.
- **Prochaine étape code** : variables d’env + route test d’envoi template, puis worker cron `channel=whatsapp` (sur le modèle Resend).

**Expiration `responded`** : cron **`service_role`** / **`/api/cron/expire-overdue-requests`** sur **`expire_overdue_requests()`** ; défaut **24 h** après **`responded_at`** (**`20260523_001`** ; surcharge Vercel **`EXPIRE_RESPONDED_SILENCE`**). **`abandon_unconfirmed_responded_requests()`** = alias.

**Notifications in-app** : marquage comptoir **`counter_outcome:picked_up`** → **aucune** insertion **`app_notifications`** (**`20260515_001`**). Libellés patient : **`20260514_001`** (surcharges **`_in_app_notification_patient`**). **Packs promo** : table dédiée **`promo_in_app_notifications`** (pas de lien `requests`) — badge cloche = demandes + promo (**`platform-header.tsx`**).

**Annuaire public (`/`)** — **`AnnuairePage`** : barre recherche/filtres **sticky** sous **`PlatformHeader`** ; hero **slate** filigrine `sm+` uniquement ; cartes **`AnnuairePharmacyCard`** — actions rapides en **colonne sur la photo** (cercles semi-opaques compacts).

**i18n patient (ar / fr — juin 2026, couverture complète vagues 1–6)** — **`CAHIER_DES_CHARGES.md` §14** + journal §10 session **2026-06-06 (suite 5)** · phrase **§13.46** : patient/public uniquement (`next-intl`, cookie `pp_locale`, switcher header, RTL) ; messages **`messages/fr/*.ts`** + **`messages/ar/*.ts`** ; helpers **`lib/i18n/`** (`build-patient-timeline`, `use-patient-datetime-formatters`, hub cards, archives, login methods) ; dates **`lib/datetime-locale.ts`** ; CI **`npm run i18n:parity`**. Couvert : conversation, pages publiques demande, hubs, détail dossier, paramètres/notifs in-app (**`20260709_001`**). Pharmacien/admin FR. SMS externes FR.

**FAB Conversation (détail dossier)** — **`RequestConversationFabDock`** sur **`app/dashboard/demandes/[id]/page.tsx`** et **`app/dashboard/pharmacien/demandes/[id]/page.tsx`** : glisser pour repositionner ; plafond haut = **`PlatformHeader`** ; **`lib/conversation-fab-position.ts`** + **`Z_FLOATING_ABOVE_STICKY_FOOTER`**. Consultation = **`RequestConversationInline`** (pas de FAB). **Messages vocaux (juin 2026)** — max **30 s** ; Storage `private-media/conversation-audio/` ; **`lib/conversation-audio-media.ts`**, **`useConversationAudioRecorder`**, **`lib/send-request-conversation-message.ts`**. **Fil dossier** (FAB modal + inline consultation) : **`ConversationComposer`** + **`ConversationMessageBubble`** + **`ConversationAudioPlayer`** — migration **`20260706_001`**. **Envoi initial patient** (demande produits, ordonnance, consultation libre) : bouton **Vocal** via **`ConversationMessageDraftField`** / **`ConversationAudioDraftPreview`** — commits **`ea54827`**, **`cb90da3`** ; pages **`/pharmacie/[id]/demande-produits`**, **`demande-ordonnance`**, **`consultation-libre`**, **`patient-demande-produits-ui.tsx`**. **MIME bucket** : migration **`20260707_001`** (audio/webm autorisé sur **`private-media`** — sans elle, upload vocal échoue). Ordre migrations conversation : **`20260705_001`** → **`20260706_001`** → **`20260707_001`**.

**UI consultation libre (juin 2026)** — commits **`c507609`**, **`081fc02`**, puis lot **2026-06-06** (**`428d662`**, **`718913f`**, **`10ddc65`**, **`bf9b94f`**) : en-têtes dossier alignés demandes produits ; fil conversation **min-height** + **scroll chaining** page (`consultationConversationMinHeightClass`, `overscroll-y-auto`) ; **`ConsultationBriefPanel`** replié « Modifier mon message », **Enregistrer les modifications** unique (texte + photos) ; édition patient jusqu’à **`responded`** (RPC **`patient_save_consultation_brief`**, migrations **`20260711_001`**, **`20260712_001`**) ; libellé **Modifié le…** sur bulle seed (`patient_content_updated_at`) ; lightbox **`ConsultationPhotoLightbox`** (Fermer + retour téléphone) ; **pas de double notif** à l’envoi avec photos (`p_initial_submit` sur attach). Après réponse pharma → onglet **Produits** ; **`RequestConversationInline`** ; migrations **`20260705_001`**. Fichiers : `consultation-brief-panel.tsx`, `consultation-photo-lightbox.tsx`, `request-conversation-inline.tsx`, `lib/platform-sticky-footer.ts`.

**Photos patient ordonnance / consultation (juin 2026, commit `bf9b94f`)** — **`lib/patient-request-photo-upload.ts`** : **compresser d’abord** (WebP ~1600 px) puis valider taille (**4 Mo** compressé, **30 Mo** brut max) ; évite faux rejets « 8 Mo » sur photos téléphone. Pages **`demande-ordonnance`**, **`consultation-libre`**, **`ConsultationBriefPanel`**, **`PatientPrescriptionEditablePanel`**, API **`prescription-page`**.

**UI ordonnances archives patient (juin 2026)** — dossier **clôturé / archive** : un seul bandeau **`PatientProductRequestDossierHeader`** (plus de **`RequestKindHeader`** doublon) — `hideMainRequestHeader` inclut **`prescription`** et **`free_consultation`** en lecture seule archive, comme demandes produits. **Annulée / abandonnée / expirée avant saisie produit** (0 ligne) : fiche complète quand même — bandeau terminal, **`PrescriptionScanCollapsible`** (ouvert par défaut), message patient, lien **Annuaire** ; pas de bouton renvoi demande produits (**`56bc5bc`**).

**UI ordonnance pharmacien (juin 2026, commit `6cb3160`)** — zoom lightbox scan, footer masqué panneau scan, dispo frozen lisible, save répondue bump **`requests.updated_at`** + refresh patient ; migrations **`20260707_002`** (sync note patient → conversation/drift), **`20260708_001`** (pas notif « mise à jour » au premier attach pages ordonnance). **Post-validé** : passage **Disponible → À commander** affiche **Réception prévue** dans le bloc produit (fix juin 2026).
